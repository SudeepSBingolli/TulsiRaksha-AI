import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tulsiraksha.app',
  appName: 'TulsiRaksha AI',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
